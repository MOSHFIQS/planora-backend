enum Role {
    USER
    ORGANIZER
    ADMIN
    SUPERADMIN
}

enum UserStatus {
    ACTIVE
    SUSPENDED
}

enum EventVisibility {
    PUBLIC
    PRIVATE
}

enum EventType {
    ONLINE
    OFFLINE
}

enum ParticipationStatus {
    PENDING
    APPROVED
    REJECTED
    BANNED
}

enum InvitationStatus {
    PENDING
    ACCEPTED
    DECLINED
}

enum PaymentStatus {
    PENDING
    SUCCESS
    FAILED
    REFUNDED
    CANCELED
    UNPAID
}

enum TicketStatus {
    VALID
    USED
    CANCELED
}

enum BannerPosition {
    MAIN
    SECONDARY
    THIRD
}

enum NotificationType {
    INFO
    WARNING
    SUCCESS
    ERROR
    INVITATION
    SYSTEM_ALERT
}

enum AuditAction {
    CREATE
    UPDATE
    DELETE
    SUSPEND
    LOGIN
    REGISTER
    PAYMENT
}
